import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ 
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-inter',
});

export const metadata: Metadata = {
  title: 'LearnHub - Modern LMS Dashboard',
  description: 'A comprehensive learning management system with modern UI and advanced analytics',
  keywords: ['LMS', 'Learning Management', 'Education', 'Dashboard'],
  authors: [{ name: 'LearnHub Team' }],
  // Remove viewport from here
};

// Add viewport as a separate export
export const viewport = {
  width: 'device-width',
  initialScale: 1,
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={inter.variable} suppressHydrationWarning>
      <body className={`${inter.className} antialiased`} suppressHydrationWarning>
        {children}
      </body>
    </html>
  );
}
